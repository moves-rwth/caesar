// RUN: bash -c 'dir=$(mktemp -d); @caesar dafny tests/boolean/dafny_comparison/insertion_sort.heyvl --dafny-dir "$dir"; out="$dir/tests/boolean/dafny_comparison/insertion_sort.dfy"; grep -q "type List(0,==)" "$out" && grep -q "function insert_sorted" "$out" && grep -q "function insertion_sorted" "$out" && grep -q "method insertion_sort" "$out" && grep -q "method insertion_sort_example" "$out"'
