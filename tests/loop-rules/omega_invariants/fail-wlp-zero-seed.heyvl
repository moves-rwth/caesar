// RUN: bash -c '@caesar @file 2>&1 | grep -q "Counter-example to verification found"'

// This loop has wlp[loop](0) = 1.
// I_0 = 0 must fail the base case Psi_f(1) <= I_0.
@wlp
coproc omega_wlp_zero_seed() -> ()
    pre 0
    post 0
{
    @omega_invariant(n, 0)
    while true {}
}
